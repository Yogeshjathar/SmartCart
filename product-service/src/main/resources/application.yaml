spring:
  application:
    name: product-service

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

eureka:
  instance:
    metadata-map:
      version: 1.0
      environment: ${spring.profiles.active}

management:
  endpoints:
    web:
      base-path: /actuator

swagger:
  title: Product Service API
  description: APIs for managing products in SmartCart
  version: v1.0